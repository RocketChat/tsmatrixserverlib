var ed = require("ed25519-supercop");
var HashMap = require("hashmap");

var KeyID;
var message;

function SignJson( signingName,KeyID,PrivateKey,message){
	var signatures;
	var map = new HashMap();
	map.set(signatures,KeyID);
	var error = new Error("there is an error oops!!");
	if (error){
		JSON.parse(message);
	}

	if (error!=nil){
		return nil,error;
	}
}

